"use client";

import { useEffect, useState, type FormEvent, type ReactNode } from "react";
import { ArrowLeft, ClipboardList, FolderCog, Paperclip, Plus, ReceiptText, RotateCcw, WalletCards } from "lucide-react";
import { CurrencyInput } from "@/components/currency-input";
import { ERPSelect } from "@/components/erp-select";
import { UploadBox } from "@/components/upload-box";
import { IconAction, KpiCard, MoneyValue } from "@/components/erp-ui";
import { expenseButton, expenseInput, money } from "@/components/expense-sheet";
import { isProjectCost, pettyLabels } from "@/lib/petty-cash";

type Named = { id: string; name: string };
type Category = Named & { active: boolean; requiresDocument: boolean; requiresAttachment: boolean };
type Account = Named & { type: string; employeeId: string | null; balanceCents: number };
type Movement = { id: string; type: string; status: string; amountCents: number; projectId: string | null; sourceAccountId: string | null; destinationAccountId: string | null; transactionDate: string; createdAt: string; description: string; documentNumber: string | null; project: { name: string } | null; category: { name: string } | null; recordedBy: { name: string }; attachments: Named[] };
type Data = { accounts: Account[]; categories: Category[]; transactions: Movement[]; projects: Named[]; employees: Named[]; canManage: boolean };
type Drawer = "" | "transaction" | "reverse" | "category";
const today = () => new Date().toISOString().slice(0, 10);
const currentMonth = () => new Date().toISOString().slice(0, 7);
function Field({ label, children }: { label: string; children: ReactNode }) { return <label className="grid gap-2 text-xs font-bold text-slate-700"><span>{label}</span>{children}</label>; }

export function PettyCashCenter() {
  const [data, setData] = useState<Data | null>(null), [error, setError] = useState(""), [notice, setNotice] = useState(""), [busy, setBusy] = useState(false);
  const [drawer, setDrawer] = useState<Drawer>(""), [section, setSection] = useState<"ledger" | "custodies" | "categories">("ledger"), [selected, setSelected] = useState("");
  const [type, setType] = useState("FUNDING"), [allocation, setAllocation] = useState("GENERAL"), [categoryId, setCategoryId] = useState(""), [accountId, setAccountId] = useState(""), [month, setMonth] = useState(currentMonth()), [projectId, setProjectId] = useState(""), [filterType, setFilterType] = useState("");
  const refresh = async () => { const response = await fetch("/api/petty-cash"); const body = await response.json(); if (!response.ok) throw new Error(body.error || "تعذر تحميل الدفتر."); setData(body); };
  useEffect(() => { void Promise.resolve().then(refresh).catch((reason) => setError(reason instanceof Error ? reason.message : "تعذر التحميل.")); }, []);
  async function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); setBusy(true); setError(""); try { const response = await fetch("/api/petty-cash", { method: "POST", body: new FormData(event.currentTarget) }); const body = await response.json(); if (!response.ok) throw new Error(body.error || "تعذر الحفظ."); await refresh(); setDrawer(""); setSelected(""); setNotice("تم حفظ الحركة وتحديث رصيد الدفتر."); } catch (reason) { setError(reason instanceof Error ? reason.message : "تعذر الحفظ."); } finally { setBusy(false); } }
  if (!data) return <p role="status">{error || "جارٍ تحميل دفتر النثريات…"}</p>;
  const main = data.accounts.find((account) => account.type === "MAIN"), custodies = data.accounts.filter((account) => account.type === "CUSTODY"), activeAccountId = accountId || main?.id || "", activeAccount = data.accounts.find((account) => account.id === activeAccountId), category = data.categories.find((item) => item.id === categoryId), original = data.transactions.find((item) => item.id === selected), editedCategory = data.categories.find((item) => item.id === selected), expense = isProjectCost(type);
  const accountRows = data.transactions.filter((item) => item.sourceAccountId === activeAccountId || item.destinationAccountId === activeAccountId).sort((a, b) => { const byDate = a.transactionDate.localeCompare(b.transactionDate); if (byDate) return byDate; const aFunding = ["OPENING_BALANCE", "FUNDING"].includes(a.type) ? 0 : 1, bFunding = ["OPENING_BALANCE", "FUNDING"].includes(b.type) ? 0 : 1; return aFunding - bFunding || a.createdAt.localeCompare(b.createdAt); });
  const ledger = accountRows.reduce<{ transaction: Movement; incoming: number; outgoing: number; balance: number }[]>((all, transaction) => { const incoming = transaction.status === "POSTED" && transaction.destinationAccountId === activeAccountId ? transaction.amountCents : 0, outgoing = transaction.status === "POSTED" && transaction.sourceAccountId === activeAccountId ? transaction.amountCents : 0, balance = (all.at(-1)?.balance ?? 0) + incoming - outgoing; return [...all, { transaction, incoming, outgoing, balance }]; }, []);
  const visible = ledger.filter((row) => (!month || row.transaction.transactionDate.startsWith(month)) && (!projectId || row.transaction.projectId === projectId) && (!filterType || row.transaction.type === filterType));
  const received = visible.reduce((sum, row) => sum + (row.transaction.status === "POSTED" && ["FUNDING", "OPENING_BALANCE"].includes(row.transaction.type) ? row.incoming : 0), 0), spent = visible.reduce((sum, row) => sum + (row.transaction.status === "POSTED" && isProjectCost(row.transaction.type) ? row.outgoing : 0), 0);
  const open = (next: Drawer, id = "") => { setDrawer(next); setSelected(id); setError(""); setNotice(""); };
  return <div className="space-y-5"><section className="flex flex-wrap items-end justify-between gap-3"><div><p className="text-xs font-bold text-blue-700">Petty Cash · النثريات والصندوق</p><h1 className="mt-1 text-2xl font-black">دفتر الصندوق التشغيلي</h1><p className="mt-2 text-sm text-slate-500">للمصروفات الصغيرة والعاجلة فقط؛ العهدة لا تصبح تكلفة إلا عند تسويتها بالإثبات.</p></div>{data.canManage && <button onClick={() => open("transaction")} className="inline-flex h-10 items-center gap-2 rounded-lg bg-blue-700 px-4 text-sm font-bold text-white"><Plus className="size-4" />إضافة حركة</button>}</section>{error && <p role="alert" className="rounded-xl border border-red-200 bg-red-50 p-3 text-sm font-bold text-red-700">{error}</p>}{notice && <p role="status" className="rounded-xl border border-emerald-200 bg-emerald-50 p-3 text-sm font-bold text-emerald-700">{notice}</p>}
    <nav className="flex flex-wrap gap-2"><Tab active={section === "ledger"} icon={ClipboardList} onClick={() => setSection("ledger")}>دفتر الحركة</Tab><Tab active={section === "custodies"} icon={WalletCards} onClick={() => setSection("custodies")}>العهد</Tab><Tab active={section === "categories"} icon={FolderCog} onClick={() => setSection("categories")}>التصنيفات</Tab></nav>
    {section === "ledger" && <><section className="grid gap-3 md:grid-cols-4"><KpiCard label="رصيد الحساب المختار" value={`${money(activeAccount?.balanceCents ?? 0)} ج.م`} icon={WalletCards} tone="blue" /><KpiCard label="وارد الشهر" value={`${money(received)} ج.م`} icon={ReceiptText} tone="emerald" /><KpiCard label="منصرف الشهر" value={`${money(spent)} ج.م`} icon={ArrowLeft} tone="amber" /><KpiCard label="العهد القائمة" value={`${money(custodies.reduce((sum, account) => sum + account.balanceCents, 0))} ج.م`} icon={WalletCards} tone="violet" /></section><section className="erp-table-shell"><div className="grid gap-3 border-b bg-white p-4 md:grid-cols-4"><Field label="الحساب"><ERPSelect value={activeAccountId} onValueChange={setAccountId}><option value={main?.id || ""}>{main?.name || "الصندوق الرئيسي"}</option>{custodies.map((account) => <option key={account.id} value={account.id}>{account.name}</option>)}</ERPSelect></Field><Field label="الشهر"><input type="month" value={month} onChange={(event) => setMonth(event.target.value)} className={expenseInput} /></Field><Field label="المشروع / عام الشركة"><ERPSelect value={projectId} onValueChange={setProjectId}><option value="">كل المشروعات والعام</option>{data.projects.map((project) => <option key={project.id} value={project.id}>{project.name}</option>)}</ERPSelect></Field><Field label="نوع الحركة"><ERPSelect value={filterType} onValueChange={setFilterType}><option value="">كل الحركات</option>{Object.entries(pettyLabels).map(([key, label]) => <option key={key} value={key}>{label}</option>)}</ERPSelect></Field></div><div className="erp-table-scroll"><table className="erp-data-table erp-responsive-table min-w-[1100px]"><thead><tr>{["التاريخ", "رقم المستند", "البيان", "المشروع / العام", "التصنيف", "وارد", "منصرف", "الرصيد بعد الحركة", "المرفق", "المسجل", "الإجراءات"].map((label) => <th key={label}>{label}</th>)}</tr></thead><tbody>{visible.slice().reverse().map(({ transaction, incoming, outgoing, balance }) => <tr key={transaction.id}><td data-label="التاريخ">{transaction.transactionDate.slice(0, 10)}</td><td data-label="رقم المستند">{transaction.documentNumber || "—"}</td><td data-label="البيان"><b>{transaction.description}</b><p className="mt-1 text-[11px] text-slate-400">{pettyLabels[transaction.type] || transaction.type}{transaction.status === "REVERSED" ? " · معكوسة" : ""}</p></td><td data-label="المشروع / العام">{transaction.project?.name || (isProjectCost(transaction.type) ? "عام الشركة" : "—")}</td><td data-label="التصنيف">{transaction.category?.name || "—"}</td><td data-label="وارد" className="text-center">{incoming ? <MoneyValue>{money(incoming)} ج.م</MoneyValue> : "—"}</td><td data-label="منصرف" className="text-center">{outgoing ? <MoneyValue>{money(outgoing)} ج.م</MoneyValue> : "—"}</td><td data-label="الرصيد بعد الحركة" className="text-center"><MoneyValue>{money(balance)} ج.م</MoneyValue></td><td data-label="المرفق" className="text-center">{transaction.attachments.length ? <a title="فتح المرفق" aria-label="فتح المرفق" className="inline-flex text-blue-700" href={`/api/petty-cash/attachments/${transaction.attachments[0].id}`} target="_blank" rel="noreferrer"><Paperclip className="size-4" /></a> : "—"}</td><td data-label="المسجل">{transaction.recordedBy.name}</td><td data-label="الإجراءات">{data.canManage && transaction.status === "POSTED" ? <IconAction label="عكس وتصحيح الحركة" icon={RotateCcw} tone="danger" onClick={() => open("reverse", transaction.id)} /> : "—"}</td></tr>)}{!visible.length && <tr><td colSpan={11} className="p-10 text-center text-slate-400">لا توجد حركات ضمن الفلاتر الحالية.</td></tr>}</tbody></table></div></section></>}
    {section === "custodies" && <SimpleTable title="العهد القائمة" note="العهدة ليست تكلفة مشروع؛ يظهر المصروف عند تسوية العهدة فقط."><table className="erp-data-table erp-responsive-table min-w-[660px]"><thead><tr>{["العهدة", "الموظف", "الرصيد", "الحالة", "الإجراء"].map((label) => <th key={label}>{label}</th>)}</tr></thead><tbody>{custodies.map((account) => <tr key={account.id}><td data-label="العهدة"><b>{account.name}</b></td><td data-label="الموظف">{data.employees.find((employee) => employee.id === account.employeeId)?.name || "—"}</td><td data-label="الرصيد" className="text-center"><MoneyValue>{money(account.balanceCents)} ج.م</MoneyValue></td><td data-label="الحالة">{account.balanceCents ? "قائمة" : "مسوّاة"}</td><td data-label="الإجراء"><button className="text-sm font-bold text-blue-700" onClick={() => { setAccountId(account.id); setSection("ledger"); }}>فتح دفتر العهدة</button></td></tr>)}{!custodies.length && <tr><td colSpan={5} className="p-10 text-center">لا توجد عهد.</td></tr>}</tbody></table></SimpleTable>}
    {section === "categories" && <section className="rounded-2xl border bg-white p-5"><div className="flex flex-wrap items-center justify-between gap-3"><div><h2 className="font-black">تصنيفات النثريات</h2><p className="mt-1 text-xs text-slate-500">تحدد لكل تصنيف إلزامية المرفق ورقم المستند.</p></div>{data.canManage && <button className={expenseButton} onClick={() => open("category")}>إضافة تصنيف</button>}</div><div className="mt-4 grid gap-3 md:grid-cols-2">{data.categories.map((item) => <article key={item.id} className="rounded-xl border p-4"><b>{item.name}</b><p className="mt-2 text-xs text-slate-500">رقم المستند: {item.requiresDocument ? "إلزامي" : "اختياري"} · المرفق: {item.requiresAttachment ? "إلزامي" : "اختياري"}</p>{data.canManage && <button className="mt-3 text-xs font-bold text-blue-700" onClick={() => open("category", item.id)}>تعديل</button>}</article>)}</div></section>}
    {drawer && data.canManage && <Drawer title={drawer === "transaction" ? "إضافة حركة" : drawer === "reverse" ? "عكس وتصحيح حركة" : "إدارة تصنيف"} onClose={() => setDrawer("")}><form key={`${drawer}-${selected}`} onSubmit={submit} className="space-y-4"><input type="hidden" name="action" value={drawer} />{drawer === "transaction" && <><Field label="نوع الحركة"><ERPSelect name="type" value={type} onValueChange={setType}>{Object.entries(pettyLabels).filter(([key]) => !key.startsWith("ADJUSTMENT")).map(([key, label]) => <option key={key} value={key}>{label}</option>)}</ERPSelect></Field><div className="grid gap-3 sm:grid-cols-2"><Field label="القيمة *"><CurrencyInput name="amount" required min={0.01} /></Field><Field label="التاريخ *"><input name="date" type="date" required defaultValue={today()} className={expenseInput} /></Field></div>{type === "FUNDING" && <p className="rounded-lg bg-blue-50 p-3 text-xs text-blue-800">مصدر التمويل ثابت: المدير التنفيذي. أرفق إثبات استلام التمويل.</p>}{type === "CUSTODY_ISSUE" && <div className="grid gap-3 sm:grid-cols-2"><Field label="الموظف *"><ERPSelect name="employeeId" required><option value="">اختر الموظف</option>{data.employees.map((employee) => <option key={employee.id} value={employee.id}>{employee.name}</option>)}</ERPSelect></Field><Field label="اسم العهدة"><input name="custodyName" className={expenseInput} placeholder="اختياري" /></Field></div>}{["CUSTODY_EXPENSE", "CUSTODY_RETURN"].includes(type) && <Field label="العهدة *"><ERPSelect name="sourceAccountId" required><option value="">اختر العهدة</option>{custodies.filter((account) => account.balanceCents > 0).map((account) => <option key={account.id} value={account.id}>{account.name} — {money(account.balanceCents)} ج.م</option>)}</ERPSelect></Field>}{expense && <><Field label="التحميل"><ERPSelect name="allocation" value={allocation} onValueChange={setAllocation}><option value="GENERAL">عام الشركة</option><option value="PROJECT">مشروع</option></ERPSelect></Field>{allocation === "PROJECT" && <Field label="المشروع *"><ERPSelect name="projectId" required><option value="">اختر المشروع</option>{data.projects.map((project) => <option key={project.id} value={project.id}>{project.name}</option>)}</ERPSelect></Field>}<Field label="التصنيف *"><ERPSelect name="categoryId" value={categoryId} onValueChange={setCategoryId} required><option value="">اختر التصنيف</option>{data.categories.filter((item) => item.active).map((item) => <option key={item.id} value={item.id}>{item.name}</option>)}</ERPSelect></Field></>}<Field label="رقم المستند"><input name="documentNumber" className={expenseInput} required={expense && category?.requiresDocument} /></Field><Field label="البيان *"><input name="description" required className={expenseInput} /></Field><UploadBox name="files" label="إثبات الحركة" required={!expense || (category?.requiresAttachment ?? true)} /></>}{drawer === "reverse" && original && <><input type="hidden" name="id" value={selected} /><p className="rounded-lg bg-amber-50 p-3">{original.description} · {money(original.amountCents)} ج.م</p><Field label="سبب العكس *"><input name="reason" required className={expenseInput} /></Field><UploadBox name="files" label="إثبات العكس" required /></>}{drawer === "category" && <><input type="hidden" name="id" value={selected} /><Field label="اسم التصنيف *"><input name="name" required defaultValue={editedCategory?.name} className={expenseInput} /></Field>{[["active", "نشط", editedCategory?.active ?? true], ["requiresDocument", "رقم المستند إلزامي", editedCategory?.requiresDocument ?? false], ["requiresAttachment", "المرفق إلزامي", editedCategory?.requiresAttachment ?? true]].map(([name, label, checked]) => <label key={String(name)} className="flex gap-2 text-sm"><input type="checkbox" name={String(name)} value="true" defaultChecked={Boolean(checked)} />{label}</label>)}</>}<button disabled={busy} className={`${expenseButton} !border-blue-700 !bg-blue-700 !text-white`}>{busy ? "جارٍ الحفظ…" : "تأكيد وحفظ"}</button></form></Drawer>}</div>;
}
function Tab({ active, icon: Icon, onClick, children }: { active: boolean; icon: typeof ClipboardList; onClick: () => void; children: ReactNode }) { return <button onClick={onClick} className={`inline-flex items-center gap-2 rounded-lg border px-4 py-2 text-sm font-bold ${active ? "border-blue-700 bg-blue-700 text-white" : "bg-white text-slate-600"}`}><Icon className="size-4" />{children}</button>; }
function SimpleTable({ title, note, children }: { title: string; note: string; children: ReactNode }) { return <section className="erp-table-shell"><div className="border-b bg-white p-4"><h2 className="font-black">{title}</h2><p className="mt-1 text-xs text-slate-500">{note}</p></div><div className="erp-table-scroll">{children}</div></section>; }
function Drawer({ title, onClose, children }: { title: string; onClose: () => void; children: ReactNode }) { return <div className="fixed inset-0 z-50 bg-slate-950/30" role="dialog" aria-modal="true" aria-label={title}><aside className="absolute inset-y-0 right-0 w-full max-w-xl overflow-y-auto bg-white p-5 shadow-2xl"><div className="flex items-center justify-between border-b pb-4"><h2 className="text-lg font-black">{title}</h2><button type="button" className={expenseButton} onClick={onClose}>إغلاق</button></div><div className="py-5">{children}</div></aside></div>; }
